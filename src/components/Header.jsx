import { Typography, Layout } from "antd";
const { Title } = Typography;
export const Header = () => {
  return (
    <Layout.Header>
      <Title level={3}>Header</Title>
    </Layout.Header>
  );
};
